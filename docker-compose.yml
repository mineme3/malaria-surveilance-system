services:
  malaria-pwa:
    build: .
    container_name: malaria-pwa
    ports:
      - "3001:3001"
    environment:
      - PORT=3001
      - JWT_SECRET=malaria-pwa-secret-key-change-in-production
      - NODE_ENV=production
      - DATABASE_URL=${DATABASE_URL}
    restart: unless-stopped
